import React from "react";

const Profile = () => {
  return (
    <div className="branches flex w-full h-full overflow-y-scroll no-scrollbar">
       Profile
    </div>
  );
};

export default Profile;
